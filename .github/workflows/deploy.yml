name: Build & Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

env:
  IMAGE_NAME: shop-fe
  CONTAINER_NAME: shop-fe
  HOST_PORT: 80

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to container registry
        uses: docker/login-action@v3
        with:
          registry: ${{ secrets.REGISTRY }}
          username: ${{ secrets.REGISTRY_USERNAME }}
          password: ${{ secrets.REGISTRY_PASSWORD }}

      - name: Compute image tags
        id: meta
        run: |
          IMAGE="${{ secrets.REGISTRY }}/${{ secrets.REGISTRY_NAMESPACE }}/${{ env.IMAGE_NAME }}"
          echo "image=$IMAGE" >> $GITHUB_OUTPUT
          echo "sha_tag=$IMAGE:${GITHUB_SHA::7}" >> $GITHUB_OUTPUT
          echo "latest_tag=$IMAGE:latest" >> $GITHUB_OUTPUT

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ${{ steps.meta.outputs.sha_tag }}
            ${{ steps.meta.outputs.latest_tag }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Deploy on server via SSH
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          port: ${{ secrets.SSH_PORT }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            set -e
            echo "${{ secrets.REGISTRY_PASSWORD }}" | docker login ${{ secrets.REGISTRY }} -u "${{ secrets.REGISTRY_USERNAME }}" --password-stdin
            docker pull ${{ steps.meta.outputs.latest_tag }}
            docker rm -f ${{ env.CONTAINER_NAME }} 2>/dev/null || true
            docker network inspect shop-network >/dev/null 2>&1 || docker network create shop-network
            docker run -d \
              --name ${{ env.CONTAINER_NAME }} \
              --network shop-network \
              --restart unless-stopped \
              -p ${{ env.HOST_PORT }}:80 \
              ${{ steps.meta.outputs.latest_tag }}
            for i in $(seq 1 30); do
              STATUS=$(docker inspect -f '{{.State.Health.Status}}' ${{ env.CONTAINER_NAME }} 2>/dev/null || true)
              [ "$STATUS" = "healthy" ] && break
              if [ "$STATUS" = "unhealthy" ]; then docker logs ${{ env.CONTAINER_NAME }}; exit 1; fi
              sleep 2
            done
            [ "$(docker inspect -f '{{.State.Health.Status}}' ${{ env.CONTAINER_NAME }})" = "healthy" ] || {
              docker logs ${{ env.CONTAINER_NAME }}
              exit 1
            }
            docker image prune -f
